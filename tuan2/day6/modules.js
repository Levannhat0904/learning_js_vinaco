const sum = function sum(a, b) {
    return a + b;
}
const minus = function minus(a, b) {
    return a - b;
}
function toUpperCase(txt){
    return txt.toUpperCase();
}
export default toUpperCase;
export const tong = sum;
export const hieu = minus;